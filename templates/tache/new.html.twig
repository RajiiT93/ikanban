{% extends 'base.html.twig' %}

{% block title %}Ajouter une tâche - {{ projet.nom }}{% endblock %}

{% block monTitre %}
    ➕ Nouvelle tâche pour le projet "{{ projet.nom }}"
{% endblock %}

{% block body %}
<div class="container mt-5" style="max-width: 700px;">
    <div class="card shadow-sm p-4">
        <h2 class="mb-4 text-primary text-center">📝 Détail de la tâche</h2>

        {{ form_start(form) }}
            <div class="form-group mb-3">
                {{ form_label(form.titre) }}
                {{ form_widget(form.titre, { 'attr': { 'class': 'form-control', 'placeholder': 'Titre de la tâche' } }) }}
            </div>

            <div class="form-group mb-3">
                {{ form_label(form.description) }}
                {{ form_widget(form.description, { 'attr': { 'class': 'form-control', 'rows': 3, 'placeholder': 'Description...' } }) }}
            </div>

            <div class="form-group mb-3">
                {{ form_label(form.statut) }}
                {{ form_widget(form.statut, { 'attr': { 'class': 'form-select' } }) }}
            </div>

            <div class="form-group mb-3">
                {{ form_label(form.deadline) }}
                {{ form_widget(form.deadline, { 'attr': { 'class': 'form-control datepicker' } }) }}
            </div>

            <div class="d-flex justify-content-between align-items-center mt-4">
                <a href="{{ path('app_projet_show', {'id': projet.id}) }}" class="btn btn-secondary">⬅️ Retour</a>
                <button type="submit" class="btn btn-success px-4">💾 Ajouter</button>
            </div>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        h2 {
            font-weight: bold;
        }

        .form-control,
        .form-select {
            border-radius: 8px;
            padding: 10px;
            font-size: 1rem;
            border: 1px solid #ced4da;
        }

        .form-control:focus,
        .form-select:focus {
            border-color: #007bff;
            box-shadow: 0 0 5px rgba(0, 123, 255, 0.3);
        }

        .btn-success {
            font-weight: bold;
            text-transform: uppercase;
            border-radius: 25px;
        }
    </style>
{% endblock %}
